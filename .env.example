# Defaults to the folder containing install.sh / this repo.
# PROJECT_ROOT=/home/pi/Projects/rpi_vpn_hotspot

# Network interface that will act as the Wi-Fi hotspot.
# On a Pi Zero this is usually wlan0.
HOTSPOT_INTERFACE=wlan0

# Static IP + prefix for the hotspot interface.
# This should match what start-hotspot-pizero configures.
HOTSPOT_STATIC_CIDR=192.168.50.1/24

# LAN interface that stays on your normal network / base IP.
# Typically eth0 on a Pi unless you got some other interface like a USB wifi or something
LAN_INTERFACE=eth0

# WireGuard interface name (as used by wg-quick / systemd).
# This corresponds to /etc/wireguard/<name>.conf.
WG_INTERFACE=wg0

# Optional: path to the WireGuard config this project should manage.
# If set, install.sh will copy this file to:
#   /etc/wireguard/${WG_INTERFACE}.conf
# with secure permissions (root:root, 600).
#
# You can use an absolute path:
#   WG_CONFIG_SOURCE_PATH=/home/pi/Projects/rpi_vpn_hotspot/PCPTFORTV.conf
#
# Or a path relative to the repo root:
#   WG_CONFIG_SOURCE_PATH=PCPTFORTV.conf
WG_CONFIG_SOURCE_PATH=PTPCFORTV.conf

# Name of the systemd service that controls the hotspot.
# install.sh will create this unit and the WebUI / watcher will target it.
HOTSPOT_SERVICE_NAME=pi-vpn-hotspot.service

# Names of the services used for the hotspot stack.
# only for validation
HOSTAPD_SERVICE=hostapd
DNSMASQ_SERVICE=dnsmasq

# Optional: host/IP to ping during install to verify basic connectivity.
PING_HOST=1.1.1.1

# Optional: remote VPN host to ping as a health check for the WireGuard tunnel.
# install.sh will only attempt this AFTER checking external IPs via curl.
VPN_HEALTHCHECK_HOST=

# Optional: expected external IPs
EXPECTED_LAN_WAN_IP=
EXPECTED_VPN_WAN_IP=

# WebUI port for the Flask server.
WEBUI_PORT=8090

# Discord webhook URL for notifications.
# If empty, Discord notifications are disabled and the watcher/webui wont work
DISCORD_WEBHOOK_URL=

# Optional: interval in seconds for the watcher to poll hotspot/vpn status.
STATE_POLL_INTERVAL=10

# Optional: how long (seconds) the WebUI caches external IP checks
# before calling curl again.
WAN_CACHE_TTL=60

# Optional: override for venv location; default is <repo>/venv
# VENV_DIR=/home/pi/Projects/rpi_vpn_hotspot/venv
